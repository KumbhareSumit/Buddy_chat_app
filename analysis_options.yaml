# This file configures the analyzer, which statically analyzes Dart code to
{ analyzer: {exclude: [build/**, android/**, ios/**, web/**, windows/**, macos/**]}, include: package:flutter_lints/flutter.yaml, linter: { rules: } }
